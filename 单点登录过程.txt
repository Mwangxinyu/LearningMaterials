主系统   -> 子系统

             -> 准备一个空白页或者其他中间页 在这个页面里面请求接口

	后端返回标识例如401吧 定义成未登录的状态  然后重定向到子系统(带系统id，回调地址)

		->主系统进行登录->登录成功->调回调地址, 拼接参数 例如code (回调地址?code =***)

		访问前端第二个地址(回调地址) 前端通过访问的地址取参数 
			
			取到后请求后端接口

  			 等待后端校验返回状态
				返回 啥啥啥 跳转到首页 登录成功
				返回 啥啥啥 认证失败 跳转 错误页
